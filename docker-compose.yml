services:
  algo:
    build: .
    container_name: okx_0dte_straddle
    restart: always
    env_file: .env
    environment:
      - PYTHONUNBUFFERED=1
      - TZ=UTC
      - ENTRY_NOW=${ENTRY_NOW:-false}
    volumes:
      - ./state:/app/state
      - ./logs:/app/logs
      - ./.env:/app/.env
